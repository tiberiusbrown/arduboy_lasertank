{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Arduboy File",
            "type": "shell",
            "command": "abcc src/main.abc -b build/fxdata.bin -u -a build/LaserTank.arduboy",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
